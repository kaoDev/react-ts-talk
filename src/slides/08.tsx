import * as React from "react"
import { Text, Heading, Slide, Appear, Cite, BlockQuote } from "spectacle"

export default (
  <Slide>
    <Heading fit caps lineHeight={1} textColor="tertiary">
      But why with React.JS?
    </Heading>
  </Slide>
)
